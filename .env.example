# =============================================================================
# FnMCP.Nexus Environment Configuration
# =============================================================================
#
# This is an example configuration file. Copy this file to .env and fill in
# your actual values. The .env file is gitignored and will not be committed.
#
# To use:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in the .env file
# 3. The .env file will be automatically loaded when the application starts
#
# =============================================================================

# -----------------------------------------------------------------------------
# Qdrant Vector Database Configuration
# -----------------------------------------------------------------------------
# URL of your Qdrant instance
# Default: http://66.179.208.238:6333
QDRANT_URL=http://66.179.208.238:6333

# API key for authenticating with Qdrant (if required)
# Leave empty if your Qdrant instance doesn't require authentication
# Default: empty string
QDRANT_API_KEY=

# Name of the Qdrant collection containing conversation embeddings
# This should match the collection name you created when setting up Qdrant
# Default: conversations
QDRANT_COLLECTION=conversations

# -----------------------------------------------------------------------------
# Embedding Service Configuration
# -----------------------------------------------------------------------------
# URL of the embedding API endpoint
# This service generates vector embeddings from text using all-MiniLM-L6-v2
# Default: http://66.179.208.238:5000/embed
EMBEDDING_API_URL=http://66.179.208.238:5000/embed

# -----------------------------------------------------------------------------
# Context Library Configuration
# -----------------------------------------------------------------------------
# Path to your context library root directory
# This is where events, projections, and other context data are stored
#
# IMPORTANT: Use absolute paths if your data directory is outside the project
# - Relative path example: ./context-library (only works if data is in project)
# - Absolute path example: /home/linux/Nexus-Data (recommended for external data)
#
# Default: ./context-library (relative to the application directory)
CONTEXT_LIBRARY_PATH=./context-library

# -----------------------------------------------------------------------------
# Optional: Logging Configuration
# -----------------------------------------------------------------------------
# Log level: Trace, Debug, Info, Warning, Error, Fatal
# Default: Info
# LOG_LEVEL=Info

# Enable verbose logging for debugging
# Default: false
# VERBOSE_LOGGING=false
